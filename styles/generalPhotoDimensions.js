import { StyleSheet } from "react-native";

export const generalPhotoDimensions = StyleSheet.create({
  dimensions: {
    width: 200,
    height: 200,
    resizeMode: "contain",
    borderWidth: 2,
    borderColor: "red",
  },
});
